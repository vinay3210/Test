while true; do swift -e "import Cocoa; let loc = NSEvent.mouseLocation; let ScreenHeight = NSScreen.main?.frame.size.height ?? 1080; let y = ScreenHeight - loc.y; CGWarpMouseCursorPosition(CGPoint(x: loc.x + 1, y: y)); Thread.sleep(forTimeInterval: 0.1); CGWarpMouseCursorPosition(CGPoint(x: loc.x, y: y))"; sleep 240; done
